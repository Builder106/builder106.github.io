% Shared certificate links — factored out so a URL change (or a cert
% re-upload) only needs updating once, not once per resume file.
\newcommand{\exampleCertLinkOne}{https://example.com/certificates/data-analytics}
\newcommand{\exampleCertLinkTwo}{https://example.com/certificates/cloud-fundamentals}
